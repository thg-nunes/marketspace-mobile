import { InputErrorMessage } from './inputErrorMessage'
import { PasswordShowButton } from './passwordShowButton'
import { RootInput } from './root'

export const Input = {
  Root: RootInput,
  ErrorMessage: InputErrorMessage,
  PasswordShow: PasswordShowButton
}
